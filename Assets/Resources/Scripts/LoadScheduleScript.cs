using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadScheduleScript : MonoBehaviour
{
    //On Startup load a JSON file with the schedule and pull those into the 
}
